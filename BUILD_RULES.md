# research-ethics 构建与发布规则

## 分层边界

1. `references/registration-tree.yaml` 是平台字段和条件规则的唯一事实源；树、填写稿、Word 与测试均应由它和脱敏 intake 机械生成。
2. 本地构建目录可以保存用户授权的私有输入和交付物，但它不是公开发布目录，绝不能整体复制到公开仓库。
3. 公开版只能通过 `scripts/prepare_public_release.py` 的白名单产生；仅包含脱敏规则、合成测试数据、脚本、测试与说明。
4. 已安装的 Codex skill 必须从已校验的 Git 源码副本复制，并在复制前后比较文件清单与 SHA-256 哈希。
5. `actual_submission` 可在用户授权的本地私有工作区使用真实申报资料；`test_public` 必须脱敏。两种模式的输入、输出、缓存和发布目录不得混用。

## 规则与验证

1. 规则变更必须同步更新账本或覆盖矩阵，并运行 YAML、条件引用、产物一致性和隐私校验。
2. V1 仅支持研究者发起的观察性研究。干预性或产品注册请求必须明确标为 `deferred_to_v2`，不得套用观察性结论。
3. `fully_live_verified`、`sample_verified`、`assumption_expanded`、`inferred_from_initial_tree`、`out_of_scope_or_blocked` 与 `deferred_to_v2` 必须如实保留，不能互相夸大。
4. 填写稿只提供可复制文本、选择说明、实时字典提示和附件准备清单；不得替用户保存、提交、完成或上传。
5. 生成填写稿前必须先通过结构性确认门槛：所有当前可见且会改变页面、字段、必填性、选项、重复组或附件的选择，均须由用户明确确认并写入 intake。研究计划书只能提供候选建议，不能代替用户确认。

## 隐私与安全

1. 只保留脱敏结构规则与合成测试数据；不得保留网页源码、截图、Cookie、凭据、真实账号、真实单位、真实联系方式或真实研究资料。
2. 所有公开发布前均须通过敏感信息扫描；扫描发现疑点时，停止发布并改用更小的白名单。
3. 平台验证只可在未保存测试草稿中进行；禁止保存、提交、完成、上传附件或删除既有研究。
